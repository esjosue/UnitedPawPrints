@model IEnumerable<pawprint.Models.tbl_adopciones>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_animales.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_usuarios.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_animales.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_usuarios.Nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdAdopcion }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.IdAdopcion }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdAdopcion })
        </td>
    </tr>
}

</table>
